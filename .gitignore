node_modules/
dist/
.netlify/
.env
.env.local
*.local
*.tsbuildinfo
.DS_Store

# Local research warehouse — DuckDB store, Parquet extracts and venv.
# All of it is derived and re-fetchable (from Supabase or Alpaca), so it
# is deliberately not version-controlled and not backed up.
research/.venv/
research/data/

# Supabase CLI link state (`supabase link`) — machine-local.
supabase/.temp/

# Supabase CLI scratch (created by `supabase init`); config only, no secrets,
# but the linked project ref and temp state are machine-local.
supabase/.temp/
supabase/.branches/
